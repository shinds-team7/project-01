<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.petnow.mapper.MyProfileMapper">

    <select id="findById" resultType="com.example.petnow.entity.User">
        SELECT id,
               email,
               nickname,
               password,
               phone,
               profile_image_url,
               provider
        FROM users
        WHERE id = #{userId} AND deleted_at IS NULL
    </select>

    <select id="existsActiveByNicknameExcludingUser" resultType="boolean">
        SELECT CASE WHEN COUNT(*) > 0 THEN TRUE ELSE FALSE END
        FROM users
        WHERE nickname = #{nickname}
          AND id != #{userId}
          AND deleted_at IS NULL
    </select>

    <update id="updateProfile">
        UPDATE users
        SET nickname = #{nickname},
            phone = #{phone},
            profile_image_url = #{profileImageUrl}
        WHERE id = #{userId} AND deleted_at IS NULL
    </update>

    <update id="updatePassword">
        UPDATE users
        SET password = #{password}
        WHERE id = #{userId} AND deleted_at IS NULL
    </update>

    <update id="withdraw">
        UPDATE users
        SET deleted_at = NOW()
        WHERE id = #{userId} AND deleted_at IS NULL
    </update>

</mapper>
