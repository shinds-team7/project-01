<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.petnow.mapper.HostMapper">
    <insert id="insert"
            parameterType="com.example.petnow.entity.Place"
            useGeneratedKeys="true"
            keyProperty="id"
            keyColumn="id">
        INSERT INTO places (
        host_user_id,
        host_user_nickname,
        name,
        description,
        place_type,
        area_size,
        capacity,
        allows_small_dog,
        allows_medium_dog,
        allows_large_dog,
        provides_home_camera,
        provides_realtime_photo,
        provides_yard,
        provides_walk,
        other_options,
        hourly_price,
        nightly_price,
        status
        ) VALUES (
        #{hostUserId},
        #{hostUserNickname},
        #{name},
        #{description},
        #{placeType},
        #{areaSize},
        #{capacity},
        #{allowsSmallDog},
        #{allowsMediumDog},
        #{allowsLargeDog},
        #{providesHomeCamera},
        #{providesRealtimePhoto},
        #{providesYard},
        #{providesWalk},
        #{otherOptions},
        #{hourlyPrice},
        #{nightlyPrice},
        #{status}
        )
    </insert>

    <select id="findAllByUserId"
            resultType="com.example.petnow.dto.response.HostPlaceListResponse">
        SELECT
        id,
        name,
        status
        FROM places
        WHERE host_user_id = #{userId}
        AND deleted_at IS NULL
        ORDER BY id DESC
    </select>

    <!--
      소프트 삭제. places 를 읽는 모든 쿼리가 deleted_at IS NULL 을 걸고 있어
      이 한 번의 갱신으로 목록·상세·호스트 목록에서 모두 사라진다.
      is_visible 도 함께 내려 공개 여부 기준으로 도는 쿼리에서도 빠지게 한다.

      host_user_id 를 WHERE 에 함께 두어 남의 장소는 갱신되지 않는다.
    -->
    <update id="softDelete">
        UPDATE places
        SET deleted_at = NOW(),
        is_visible = FALSE
        WHERE id = #{placeId}
        AND host_user_id = #{hostUserId}
        AND deleted_at IS NULL
    </update>
</mapper>
