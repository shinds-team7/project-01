<?xml version="1.0" encoding="UTF-8" ?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.petnow.mapper.PetMapper">
    <!--
      INSERT 후 pet_photos.pet_id 를 채우려면 방금 만들어진 PK 가 필요하다.
      useGeneratedKeys 가 없으면 pet.getId() 가 null 로 남아 사진을 저장할 수 없다.
    -->
    <insert id="insertPet" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO pets
        (
            user_id,
            name,
            birth_year,
            sex,
            weight,
            is_neutered,
            memo,
            size
        )
        VALUES
            (
                #{userId},
                #{name},
                #{birthYear},
                #{sex},
                #{weight},
                #{isNeutered},
                #{memo},
                #{size}
            )
    </insert>

    <select id="getPetList"
            parameterType="long"
            resultType="com.example.petnow.dto.response.PetListResponse">

        SELECT
            id,
            name,
            size,
            weight
        FROM pets
        WHERE user_id = #{userId}
        AND deleted_at IS NULL
        ORDER BY created_at DESC

    </select>

    <update id="updatePet">
        UPDATE pets
        SET name = #{name},
            birth_year = #{birthYear},
            sex = #{sex},
            size = #{size},
            weight = #{weight},
            is_neutered = #{isNeutered},
            memo = #{memo}
        WHERE id = #{petId}
          AND user_id = #{userId}
          <!-- 이미 삭제된 반려동물은 되살아나지 않는다. getDetail·deletePet 과 같은 조건. -->
          AND deleted_at IS NULL
    </update>

    <select id="getDetail"
            parameterType="long"
            resultType="com.example.petnow.dto.response.PetDetailResponse">
        SELECT
        name,
        size,
        weight,
        birth_year,
        sex,
        is_neutered,
        memo
        FROM pets
        WHERE id = #{petId}
        AND
        user_id = #{userId}
        AND deleted_at IS NULL
    </select>

    <update id="deletePet">
        UPDATE pets
        SET deleted_at = NOW()
        WHERE id = #{petId}
        AND user_id = #{userId}
        AND deleted_at IS NULL
    </update>

</mapper>