<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.petnow.mapper.UserMapper">

    <insert id="signup"
            parameterType="com.example.petnow.entity.User"
            useGeneratedKeys="true"
            keyProperty="userId">

        INSERT INTO users (
            email,
            nickname,
            password
        )
        VALUES (
            #{email},
            #{nickname},
            #{password}
        )

    </insert>

    <select id="findByEmail" resultType="com.example.petnow.entity.User">

        SELECT
            user_id,
            email,
            nickname,
            password
        FROM users
        WHERE email = #{email}
          AND deleted_at IS NULL

    </select>

</mapper>
