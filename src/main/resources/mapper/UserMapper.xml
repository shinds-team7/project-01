<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.petnow.mapper.UserMapper">
    <insert id="join" parameterType="com.example.petnow.entity.User">

        INSERT INTO users (
            email,
            nickname,
            password
        )
        VALUES (
            #{email},
            #{nickname},
            #{password}
        )

    </insert>

    <select id="findByEmail" resultType="com.example.petnow.entity.User">

        SELECT email, nickname, password
        FROM users
        WHERE email = #{email}

    </select>
</mapper>