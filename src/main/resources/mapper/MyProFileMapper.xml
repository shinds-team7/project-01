<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.petnow.mapper.MyProfileMapper">

    <!--   내 정보 상세 조회   -->
    <select id="findById" resultType="com.example.petnow.entity.User">

        SELECT email, nickname, phone
        FROM users
        WHERE id = #{userId} AND deleted_at IS NULL

    </select>

    <update id="withdraw">

        UPDATE users
        SET deleted_at = NOW()
        WHERE id = #{userID} AND deleted_at IS NULL

    </update>

</mapper>